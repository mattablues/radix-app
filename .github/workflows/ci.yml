name: CI

on:
  push:
    branches: [main, master, develop]
  pull_request:
  schedule:
    - cron: "0 2 * * *"
  workflow_dispatch:
    inputs:
      run_infection_mode:
        description: "Mode: pr or schedule"
        required: false
        default: "schedule"

env:
  ENABLE_FRONTEND_BUILD: ${{ vars.ENABLE_FRONTEND_BUILD || '1' }}
  ENABLE_INFECTION_ON_PR: ${{ vars.ENABLE_INFECTION_ON_PR || '1' }}

jobs:
  php:
    runs-on: ubuntu-latest
    env:
      RADIX_RUN_ID: "${{ github.run_id }}-${{ github.run_attempt }}"
      HEALTH_CACHE_PATH: "${{ github.workspace }}/cache/health"
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          coverage: none
          extensions: pdo_mysql, pdo_sqlite
          tools: composer
      - run: composer install --no-interaction --prefer-dist --no-progress

      - name: Prepare health cache dir
        run: mkdir -p "${{ github.workspace }}/cache/health"

      - name: Set up Node
        if: env.ENABLE_FRONTEND_BUILD == '1'
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install NPM deps
        if: env.ENABLE_FRONTEND_BUILD == '1'
        run: npm ci
      - name: Build assets
        if: env.ENABLE_FRONTEND_BUILD == '1'
        run: npm run start:build

      - run: composer format:check
      - run: composer stan

      - name: Run PHPUnit (deterministic)
        run: |
          vendor/bin/phpunit -c phpunit.xml --display-deprecations --display-errors --display-notices --do-not-cache-result
        env:
          APP_ENV: development


  # Hjälpjobb för att hitta flakiga tester på Actions
  php-random:
    if: github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    env:
      RADIX_RUN_ID: "${{ github.run_id }}-${{ github.run_attempt }}"
      HEALTH_CACHE_PATH: "${{ github.workspace }}/cache/health"
    needs: php
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          coverage: none
          tools: composer
      - run: composer install --no-interaction --prefer-dist --no-progress

      - name: Prepare health cache dir
        run: mkdir -p "${{ github.workspace }}/cache/health"

      - name: PHPUnit random order (no coverage)
        run: |
          vendor/bin/phpunit -c phpunit.xml --order-by=defects,random --resolve-dependencies --testdox
        env:
          APP_ENV: development

  infection:
    if: (github.event_name == 'pull_request' && (vars.ENABLE_INFECTION_ON_PR || '1') == '1')
        || github.event_name == 'schedule'
        || (github.event_name == 'workflow_dispatch' && inputs.run_infection_mode == 'schedule')
    needs: php
    runs-on: ubuntu-latest
    env:
      RADIX_RUN_ID: "${{ github.run_id }}-${{ github.run_attempt }}"
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          coverage: xdebug
          tools: composer
      - run: composer install --no-interaction --prefer-dist --no-progress

      - name: Pre-check PHPUnit for Infection
        env:
          XDEBUG_MODE: coverage
          APP_ENV: development
        run: |
          vendor/bin/phpunit -c phpunit.xml --display-deprecations --display-errors --display-notices --colors=never --order-by=default --do-not-cache-result

      - name: Clean caches (matchar lokal körning)
        run: |
          rm -rf .phpunit.cache build/coverage_abs build/coverage_rel || true
          rm -f vendor/bin.phpunit.result.cache || true

      - name: Dump autoload (optimize)
        run: composer dump-autoload -o

      # 1) Coverage med absolut HEALTH_CACHE_PATH (realpath sannolikt true)
      - name: Generate coverage (abs path)
        env:
          XDEBUG_MODE: coverage
          HEALTH_CACHE_PATH: "${{ github.workspace }}/cache/health"
        run: |
          mkdir -p build/coverage_abs
          vendor/bin/phpunit \
            -c phpunit.xml \
            --order-by=default \
            --do-not-cache-result \
            --coverage-xml=build/coverage_abs \
            --log-junit=build/coverage_abs/junit.xml

      # Kör Infection mot den här coverage-mappen direkt
      - name: Run Infection on abs coverage
        run: |
          test -f build/coverage_abs/index.xml
          vendor/bin/infection \
            --threads=1 \
            --ignore-msi-with-no-mutations \
            --skip-initial-tests \
            --no-interaction \
            --no-progress \
            --coverage=build/coverage_abs \
            --test-framework-options="--order-by=default --do-not-cache-result"

      # 2) Coverage med relativ HEALTH_CACHE_PATH för att forcera realpath=false-fallet
      - name: Generate coverage (relative path -> realpath=false)
        if: always()
        env:
          XDEBUG_MODE: coverage
          HEALTH_CACHE_PATH: ".__health__${{ github.run_id }}-${{ github.run_attempt }}/sub"
        run: |
          rm -rf .phpunit.cache vendor/bin.phpunit.result.cache || true
          mkdir -p build/coverage_rel
          vendor/bin/phpunit \
            -c phpunit.xml \
            --order-by=default \
            --do-not-cache-result \
            --coverage-xml=build/coverage_rel \
            --log-junit=build/coverage_rel/junit.xml

      - name: Run Infection on relative coverage
        if: always()
        run: |
          test -f build/coverage_rel/index.xml
          vendor/bin/infection \
            --threads=1 \
            --ignore-msi-with-no-mutations \
            --skip-initial-tests \
            --no-interaction \
            --no-progress \
            --coverage=build/coverage_rel \
            --test-framework-options="--order-by=default --do-not-cache-result"

      - name: Upload Infection report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: infection-report
          path: infection-report.html
          if-no-files-found: ignore