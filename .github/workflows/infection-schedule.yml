name: Infection (schedule)

on:
  schedule:
    - cron: "0 2 * * 0"
  workflow_dispatch:
    inputs:
      infection_filter:
        description: "Optional Infection --filter path (e.g. src/Models)"
        required: false
        default: ""
      infection_run_slow:
        description: "Run PHPUnit group 'slow' inside Infection (true/false)"
        required: false
        default: "false"

permissions:
  contents: read

env:
  # Standard i templated projekt: bara "src". Ändra till "" för full körning.
  INFECTION_FILTER: ""

jobs:
  infection:
    if: github.event_name == 'workflow_dispatch' || github.event_name == 'schedule'
    runs-on: ubuntu-latest
    env:
      RADIX_RUN_ID: "${{ github.run_id }}-${{ github.run_attempt }}"
    steps:
      - name: Skip schedule run unless enabled
        if: github.event_name == 'schedule' && (vars.ENABLE_INFECTION_SCHEDULE || '0') != '1'
        run: |
          echo "Schedule disabled (ENABLE_INFECTION_SCHEDULE != 1). Exiting successfully."
          exit 0

      - uses: actions/checkout@v4

      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          coverage: xdebug
          tools: composer

      - run: composer install --no-interaction --prefer-dist --no-progress

      - name: Pre-check PHPUnit for Infection
        env:
          XDEBUG_MODE: coverage
          APP_ENV: development
        run: |
          vendor/bin/phpunit -c phpunit.xml --display-deprecations --display-errors --display-notices --colors=never --order-by=default --do-not-cache-result

      - name: Generate coverage for Infection
        env:
          XDEBUG_MODE: coverage
        run: |
          rm -rf build/coverage || true
          mkdir -p build/coverage
          vendor/bin/phpunit \
            -c phpunit.xml \
            --order-by=default \
            --do-not-cache-result \
            --coverage-xml=build/coverage/coverage-xml \
            --log-junit=build/coverage/junit.xml

      - name: Run Infection
        run: |
          RAW_FILTER="${{ github.event.inputs.infection_filter || env.INFECTION_FILTER }}"
          CONFIG_FILE="infection.json.dist"
          
          if [ "$RAW_FILTER" = "src" ]; then
            echo "Limited Mode: Rebuilding config to include ONLY src/ directory..."
            jq '.source.directories = ["src"]' infection.json.dist > infection.project.json
            CONFIG_FILE="infection.project.json"
            FILTER_ARG=""
          elif [ -n "$RAW_FILTER" ]; then
            echo "Custom filter mode: $RAW_FILTER"
            FILTER_ARG="--filter=$RAW_FILTER"
          else
            echo "Full scope mode (Radix style)"
            FILTER_ARG=""
          fi

          EXCLUDE_SLOW="--exclude-group slow"
          RUN_SLOW="${{ github.event.inputs.infection_run_slow || 'false' }}"
          if [ "$RUN_SLOW" = "true" ]; then
            echo "Infection: running INCLUDING slow group"
            EXCLUDE_SLOW=""
          else
            echo "Infection: running EXCLUDING slow group"
          fi

          vendor/bin/infection \
            --configuration=$CONFIG_FILE \
            --threads=1 \
            --ignore-msi-with-no-mutations \
            --min-msi=80 \
            --min-covered-msi=90 \
            --skip-initial-tests \
            --no-interaction \
            --no-progress \
            --coverage=build/coverage \
            $FILTER_ARG \
            --test-framework-options="--configuration=phpunit.xml $EXCLUDE_SLOW --order-by=default --do-not-cache-result"

      - name: Upload Infection report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: infection-report
          path: infection-report.html
          if-no-files-found: ignore