# --- Radix Engine Configuration Example ---
# Kopiera denna fil till .env och fyll i dina lokala inställningar.

# Filter för Infection (Mutation Testing) eg. src
INFECTION_FILTER=

# --- App Settings ---
APP_ENV=development        # prod, production, dev, development, test
APP_DEBUG=1                # 1 för på, 0 för av
APP_LANG=sv                # Systemets standardspråk (t.ex. sv, en)
APP_NAME="Radix System"
APP_TIMEZONE=Europe/Stockholm
APP_URL=http://localhost
APP_COPY="Ditt Företag"    # Används i footern för copyright
APP_COPY_YEAR=2026         # Startår för copyright
APP_MAINTENANCE=0          # 1 sätter systemet i underhållsläge

# --- Access Settings ---
APP_PRIVATE=0              # 1 stänger av publik registrering (endast admin kan skapa konton)

# --- Locator Settings (Används för systemhälsa/väder/plats) ---
LOCATOR_COUNTRY=SE
LOCATOR_CITY=Stockholm
LOCATOR_CITY_URL=https://www.klart.se/se/stockholms-län/stockholm/

# --- API & Security ---
# Byt dessa till unika och långa strängar i produktion!
API_TOKEN=din-hemliga-api-token
HEALTH_REQUIRE_TOKEN=1     # Kräver API_TOKEN för att visa hälsodata via API
HEALTH_IP_ALLOWLIST=127.0.0.1,::1  # Kommaseparerad lista över tillåtna IP-adresser
TRUSTED_PROXY=             # Kommaseparerad lista över betrodda proxy-IPs

# --- CORS Settings ---
CORS_ALLOW_ORIGIN=http://localhost
CORS_ALLOW_CREDENTIALS=1   # 1 för true, 0 för false

# --- Database Settings ---
DB_DRIVER=mysql            # mysql eller sqlite
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=radix
DB_USERNAME=root
DB_PASSWORD=
DB_CHARSET=utf8mb4

# --- Session Settings ---
# VIKTIGT: Använd 'file' vid första installation/migration. 
# Ändra till 'database' först efter att 'sessions'-tabellen skapats.
SESSION_DRIVER=file        # file eller database
SESSION_FILE_PATH=/tmp/sessions # Relativ (från rot) eller absolut sökväg
SESSION_TABLE=sessions     # Endast om driver=database
SESSION_LIFETIME=1440      # Livslängd i sekunder

# --- Encryption Keys ---
# Generera långa, slumpmässiga strängar (minst 32 tecken)
SECURE_TOKEN_HMAC=byt-ut-mig-till-något-väldigt-långt
SECURE_ENCRYPTION_KEY=byt-ut-mig-till-något-väldigt-långt-32-tecken

# --- Cache Paths ---
# Systemet hanterar både relativa och absoluta sökvägar.
# Relativa sökvägar utgår från projektets rotmapp.
# Windows: cache\views  |  Linux: cache/views
CACHE_ROOT=cache
VIEWS_CACHE_PATH=cache/views
APP_CACHE_PATH=cache/app
HEALTH_CACHE_PATH=cache/health
# --- IMPORTANT!!! ---
# Windows: C:\Users\[name]\[path]\Radix\cache\ratelimit
# Linux: /var/www/[name]/cache/views
RATELIMIT_CACHE_PATH=cache/ratelimit

# --- Mail Settings (SMTP) ---
MAIL_DEBUG=0               # 1 för att se loggar vid sändning
MAIL_CHARSET=UTF-8
MAIL_HOST=smtp.mailtrap.io
MAIL_PORT=2525
MAIL_SECURE=tls            # none, ssl eller tls
MAIL_AUTH=1                # 1 för true (kräver inloggning)
MAIL_ACCOUNT=ditt-användarnamn
MAIL_PASSWORD=ditt-lösenord
MAIL_EMAIL=noreply@example.com
MAIL_FROM="Radix System"   # Avsändarnamn